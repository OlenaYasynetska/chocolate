# 🚀 Настройка современного стека для Simply Chocolate

## ✅ Что было добавлено:

### 1. **Vite** - Современный сборщик
- ⚡ Мгновенный запуск dev-сервера
- 🔥 Hot Module Replacement (HMR)
- 📦 Оптимизированная сборка

### 2. **TypeScript** - Типизированный JavaScript
- 🛡️ Статическая типизация
- 🔍 Лучший автокомплит в IDE
- 🐛 Раннее обнаружение ошибок
- 📁 Файлы: `src/ts/*.ts`

### 3. **SCSS/SASS** - CSS препроцессор
- 🔄 Переменные (`_variables.scss`)
- 🎨 Миксины (`_mixins.scss`)
- 📐 Математические операции
- 🧩 Модульная структура
- 📁 Файлы: `src/scss/*.scss`

### 4. **CSS Grid + Container Queries**
- 📐 Современные раскладки с CSS Grid
- 📱 Container Queries вместо Media Queries
- 🎯 Адаптивность по размеру контейнера

### 5. **ESLint + Prettier**
- ✅ Проверка качества кода
- 💅 Автоматическое форматирование
- 📏 Единый стиль кода

---

## 📦 Установка зависимостей:

```bash
npm install
```

---

## 🏃 Запуск проекта:

### Development режим:
```bash
npm run dev
```
- Запускает Vite dev-сервер на `http://localhost:3000`
- Hot Module Replacement (HMR) для мгновенных обновлений
- Автоматическая компиляция TypeScript и SCSS

### Production сборка:
```bash
npm run build
```
- Компилирует TypeScript
- Собирает оптимизированные файлы в `dist/`
- Минифицирует CSS и JS

### Предпросмотр production сборки:
```bash
npm run preview
```

### Проверка типов TypeScript:
```bash
npm run type-check
```

### Линтинг кода:
```bash
npm run lint
```

### Форматирование кода:
```bash
npm run format
```

---

## 📁 Структура проекта:

```
chocolate/
├── src/
│   ├── ts/              # TypeScript файлы
│   │   ├── main.ts      # Главная точка входа
│   │   ├── menu.ts      # Меню функциональность
│   │   └── modal.ts     # Модальное окно
│   └── scss/            # SCSS стили
│       ├── _variables.scss  # CSS переменные
│       ├── _mixins.scss     # Миксины
│       ├── _base.scss       # Базовые стили
│       ├── _grid-examples.scss  # CSS Grid примеры
│       └── main.scss        # Главный SCSS файл
├── css/                 # Старые CSS файлы (можно постепенно конвертировать)
├── js/                  # Старые JS файлы (заменены на TypeScript)
├── images/              # Изображения
├── index.html           # HTML главная страница
├── vite.config.js       # Конфигурация Vite
├── tsconfig.json        # Конфигурация TypeScript
├── .eslintrc.json       # Конфигурация ESLint
├── postcss.config.js    # Конфигурация PostCSS
└── package.json         # Зависимости и скрипты
```

---

## 🎨 Современные CSS технологии:

### CSS Grid примеры:
```scss
.our-products {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
  gap: 20px;
}
```

### Container Queries:
```scss
.container {
  container-type: inline-size;
  container-name: main-container;
  
  @container main-container (min-width: 768px) {
    // Стили для контейнера шириной >= 768px
  }
}
```

### SCSS переменные:
```scss
$accent-color: #FD9222;
$main-text-color: #111111;
```

### SCSS миксины:
```scss
@include grid-layout(4, 20px, true);
@include transition(color, 300ms, ease);
```

---

## 📝 Следующие шаги:

1. ✅ Установить зависимости: `npm install`
2. ✅ Запустить dev-сервер: `npm run dev`
3. 🔄 Постепенно конвертировать старые CSS в SCSS
4. 🔄 Добавить больше TypeScript типов
5. 🔄 Добавить тесты (опционально)

---

## 💡 Полезные команды:

- `npm run dev` - Запуск dev-сервера
- `npm run build` - Production сборка
- `npm run preview` - Предпросмотр сборки
- `npm run type-check` - Проверка TypeScript типов
- `npm run lint` - Проверка кода
- `npm run format` - Форматирование кода

---

## 🐛 Решение проблем:

### Если TypeScript не компилируется:
```bash
npm run type-check
```

### Если стили не применяются:
- Убедитесь, что используете SCSS через Vite
- Проверьте консоль браузера на ошибки

### Если dev-сервер не запускается:
- Проверьте, что порт 3000 свободен
- Удалите `node_modules` и выполните `npm install` заново

---

**Готово! Проект теперь использует современный стек технологий! 🎉**

